export const HeroDescription = () => {
  return (
    <div className="mt-6 sm:mt-8 animate-fade-in" style={{ animationDelay: '0.1s' }}>
      <p className="text-xl sm:text-2xl md:text-3xl font-sans font-medium text-foreground max-w-3xl mx-auto leading-relaxed">
        Trade, farm, bridge — with{' '}
        <span className="text-primary font-semibold">AI scam guards</span>{' '}
        and{' '}
        <span className="text-accent font-semibold">on-chain transparency</span>.
      </p>
    </div>
  );
};
